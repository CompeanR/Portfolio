# jcompean.com

Personal site. Vite, TypeScript, Three.js.

```bash
npm install
npm run dev
npm run build
```

The hero scene lives in `src/scene.ts` and only loads on viewports 768px and wider when the visitor has not asked for reduced motion. Screenshots and icons for the highlights are in `public/media/`.
